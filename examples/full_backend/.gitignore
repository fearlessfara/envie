# Envie generated files (auto-generated, should not be committed)
envie_override.tf
envie.generated.tf
*.envie.tf

# Terraform files
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
terraform.tfvars
*.tfplan

# Lambda dependencies
units/api/lambda/node_modules/
units/api/lambda/package-lock.json

# Build and run artifacts: the archive_file output, and whatever a deploy left
# behind. All regenerated, none of it worth cloning.
units/api/builds/
deploy.log
outputs.json

# OS files
.DS_Store
